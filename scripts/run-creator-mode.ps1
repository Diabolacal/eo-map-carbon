# Launch New Eden Creator Mode: the visual lab with the locked human baseline
# plus procedural sky, ISM, glow, persist, and the tuning window.

$ErrorActionPreference = "Stop"
& "$PSScriptRoot\run-neweden.ps1"
exit $LASTEXITCODE
